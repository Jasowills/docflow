{
  "name": "@docflow/extension",
  "version": "1.0.16.1",
  "private": true,
  "description": "DocFlow Recorder browser extension for capturing website and web app workflows",
  "scripts": {
    "build": "tsc && node build.mjs",
    "build:publish": "node publish-release.mjs",
    "watch": "tsc --watch",
    "clean": "rimraf dist DocFlow-recorder"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "@types/chrome": "^0.1.37"
  },
  "devDependencies": {
    "@azure/storage-blob": "^12.31.0",
    "archiver": "^7.0.1",
    "rimraf": "^5.0.0",
    "typescript": "~5.5.0"
  }
}
